import Link from 'next/link'; import {elements,slug} from '@/lib/recipes'; export const metadata={title:'All Infinite Craft Elements | CraftPath'}; export default function Elements(){return <><h1>All sample elements</h1><p className="muted">Browse the current verified sample dataset.</p><div className="grid">{elements.map(e=><Link className="card" href={`/recipes/${slug(e)}`} key={e}>{e}</Link>)}</div></>}
