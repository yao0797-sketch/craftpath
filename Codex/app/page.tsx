import Link from 'next/link'; import {elements,slug,findRecipes} from '@/lib/recipes';
export default function Home(){return <><section className="hero"><h1>Infinite Craft Recipes</h1><p>Find combinations and discover what to make next.</p><form className="search" action="/search"><input name="q" placeholder="Search an element, e.g. Dragon"/><button>Search</button></form><p className="muted">{elements.length} demo elements · {findRecipes('').length} verified sample recipes</p></section><div className="notice">Demo launch: recipe data is a small, clearly labelled sample. More verified data will be added before SEO expansion.</div><h2>Popular recipe searches</h2><div className="grid">{['Dragon','Human','Plant','Lava','Phoenix','Wizard'].map(x=><Link className="card" href={`/recipes/${slug(x)}`} key={x}><h3>How to make {x}</h3><span className="muted">View recipe →</span></Link>)}</div></>}
